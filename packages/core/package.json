{
  "name": "@sigcord/core",
  "repository": "https://github.com/raspberry-varg/discord-interactive-menus.git",
  "version": "7.0.0",
  "description": "Simple utility to have reactive, routed, and interactive Discord menus.",
  "directories": {
    "lib": "dist"
  },
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint .",
    "format": "npx prettier --write src",
    "prebuild": "pnpm run clean",
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepack": "pnpm run build && pnpm dlx prettier --check src"
  },
  "license": "ISC",
  "dependencies": {
    "@preact/signals-core": "^1.8.0"
  },
  "devDependencies": {
    "discord.js": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "nx": "22.0.3"
  },
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "nx": {},
  "publishConfig": {
    "access": "public"
  }
}
